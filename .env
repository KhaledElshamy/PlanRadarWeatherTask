// Secrets hold the credentials that must never be checked into source control.
// Add `PlanRadarTask/Secrets.xcconfig` to .gitignore and sync the file to each machine
// before building so the key is available through build settings.
API_KEY = f5cb0b965ea1564c50c6f1b74534d823

